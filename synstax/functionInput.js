// function input - 함수의 입력 


// round는 입력값을 반올림 하는 함수
console.log(Math.round(1.6)); // 2
console.log(Math.round(1.4)); // 1

function sum(first, second) { // parameter 매개체 
    console.log(first + second);
}

sum(2, 4); // argument 각각의 입력값 (인자)
